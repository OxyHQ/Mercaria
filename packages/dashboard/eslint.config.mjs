/**
 * ESLint for @mercaria/dashboard (#496).
 *
 * The rule set is shared with the other two Expo apps — see
 * `eslint.config.apps.mjs` at the repo root for what it contains and why this
 * package does not carry its own copy. Adding a rule HERE is how a deliberate
 * per-app divergence gets recorded; there are none today.
 */
export { default } from "../../eslint.config.apps.mjs";

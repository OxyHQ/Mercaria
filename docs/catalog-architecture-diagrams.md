# Catalog architecture diagrams

> **GENERATED — do not edit this file.** Every arrow below is derived: cardinality from
> drizzle's own table reflection, the population from the migration SQL, the module grouping
> from the gated table in [`catalog-table-ownership.md`](catalog-table-ownership.md), and write
> ownership from a scan of the production source.
>
> Regenerate: `bun run --cwd packages/backend architecture:diagrams`
> Source: `packages/backend/scripts/generate-architecture-diagrams.ts` · Gate: `packages/backend/scripts/architecture/__tests__/catalog-architecture-diagrams.test.ts`

#367 Workstream 19. The companion to
[`catalog-table-ownership.md`](catalog-table-ownership.md), which answers *who owns this table*
in prose; this answers *what is it attached to, how many of it are there, and who writes it* in
pictures. [`catalog-glossary.md`](catalog-glossary.md) carries the curated, annotated subset a
newcomer should read first — its diagram is gated against the same derivation as this one, so
the two cannot disagree.

## Why this file is generated rather than drawn

Four separate figures in `catalog-table-ownership.md` had silently rotted before #857 removed
them: a table count, a journal index, a copy of `SCHEMA_TABLE_COUNT`, and a list of migrations
that contradicted its own table on the day it was written. A diagram is a worse home for a fact
like that than a sentence is — **a wrong arrow reads as an architectural decision rather than as
an error**, so it is argued with instead of corrected.

That is not hypothetical here. When this generator was first run, its output was compared
against the hand-drawn ER diagram in `catalog-glossary.md`, and **eight edges there asserted a
mandatory parent for a foreign key whose column is nullable**. It was one systematic omission
rather than eight mistakes — that diagram never used the optional marker at all — and the
load-bearing consequence was `listings }o--|| categories`, which told every reader that a
listing always has a category. `listings.category_id` has been nullable since `0000`, confirmed
three ways: the drizzle reflection, the `.references()` call with no `.notNull()`, and the
`"category_id" text,` in the migration with no later `ALTER`. Those edges were corrected in the
same change that added this file, and that diagram is now gated against the same derivation.

## What is derived, and what a foreign key cannot tell you

Read the markers precisely; they are emitted from schema facts and nothing else.

- **`||` on the parent side** — every column of the foreign key is `NOT NULL`, so a child row
  always names a parent. Derived from column nullability.
- **`|o` on the parent side** — at least one foreign-key column is nullable, so the parent is
  optional. Derived from column nullability.
- **`o{` on the child side** — nothing in the schema bounds how many children a parent may have.
  Derived from the *absence* of a covering unique.
- **`o|` on the child side** — a unique constraint covers a **subset** of the foreign-key
  columns, so a parent has at most one child across this key. Derived from unique constraints
  and non-partial unique indexes.

**No edge ever carries `}|` or `|{` ("one or more"), and that is the honest half.** "Every
parent must have at least one child" is not expressible in a foreign key, a `NOT NULL` or a
unique index, so no marker here may claim it. A relationship you know to be mandatory from the
parent side is a fact about application code, and it belongs in prose beside the diagram rather
than in the diagram.

Three further limits, stated rather than left to be discovered:

- **Partial unique indexes are ignored.** A unique with a `WHERE` clause constrains only the
  rows matching its predicate, so reading one as a 1:1 would claim a bound the table does not
  have. Expression indexes are ignored for the same reason: a unique over `lower(name)` is not a
  unique over `name`.
- **A unique over a *superset* of the foreign-key columns proves nothing.**
  `UNIQUE(category_id, locale, normalized_alias)` permits many aliases per category; only a
  unique whose columns are all *within* the foreign key pins every one of them for a given
  parent row.
- **Column names are the SQL names**, resolved through drizzle's own `CasingCache` with the
  repository's `DATABASE_CASING`. The schema declares `categoryId`; the DDL says `category_id`.
  Re-implementing that conversion here would be a third spelling of it, free to disagree with
  the two that decide the real schema.

## 1. Module dependency graph

Each arrow is at least one foreign key from a table the module owns to a table the target owns.
Modules are the rows of the gated table in `catalog-table-ownership.md`; the right-hand column
holds the pre-existing catalogue and commerce tables the epic attaches to and does not own.

```mermaid
flowchart LR
    subgraph epic["#367 catalog modules"]
        direction TB
        M_Taxonomy["Taxonomy"]
        M_Classification["Classification"]
        M_Product_types["Product types"]
        M_Localization["Localization"]
        M_Variant_axes_and_claims["Variant axes and claims"]
        M_Authoring["Authoring"]
        M_Proposals["Proposals"]
        M_Governance["Governance"]
        M_Navigation["Navigation"]
        M_Compatibility["Compatibility"]
        M_External_mappings["External mappings"]
        M_Connector_pins["Connector pins"]
        M_Native_variant_images["Native variant images"]
    end
    subgraph outside["Pre-existing tables the epic attaches to"]
        direction TB
        X_attribute_definitions[("attribute_definitions")]
        X_attribute_enum_values[("attribute_enum_values")]
        X_brands[("brands")]
        X_canonical_product_families[("canonical_product_families")]
        X_canonical_products[("canonical_products")]
        X_canonical_variants[("canonical_variants")]
        X_catalog_sources[("catalog_sources")]
        X_categories[("categories")]
        X_collections[("collections")]
        X_connections[("connections")]
        X_listing_images[("listing_images")]
        X_listings[("listings")]
        X_product_variants[("product_variants")]
        X_source_records[("source_records")]
        X_stores[("stores")]
    end
    M_Localization --> M_Product_types
    M_Variant_axes_and_claims --> M_Product_types
    M_Authoring --> M_Product_types
    M_Proposals --> M_Authoring
    M_Proposals --> M_Product_types
    M_Proposals --> M_Variant_axes_and_claims
    M_External_mappings --> M_Product_types
    M_Taxonomy --> X_catalog_sources
    M_Taxonomy --> X_categories
    M_Classification --> X_canonical_products
    M_Classification --> X_categories
    M_Classification --> X_listings
    M_Product_types --> X_attribute_definitions
    M_Product_types --> X_categories
    M_Localization --> X_attribute_enum_values
    M_Localization --> X_canonical_product_families
    M_Localization --> X_canonical_products
    M_Localization --> X_categories
    M_Localization --> X_listings
    M_Variant_axes_and_claims --> X_attribute_definitions
    M_Variant_axes_and_claims --> X_attribute_enum_values
    M_Variant_axes_and_claims --> X_connections
    M_Variant_axes_and_claims --> X_listings
    M_Variant_axes_and_claims --> X_product_variants
    M_Authoring --> X_attribute_definitions
    M_Authoring --> X_attribute_enum_values
    M_Authoring --> X_categories
    M_Authoring --> X_listings
    M_Authoring --> X_stores
    M_Proposals --> X_attribute_definitions
    M_Proposals --> X_categories
    M_Proposals --> X_stores
    M_Navigation --> X_brands
    M_Navigation --> X_canonical_product_families
    M_Navigation --> X_categories
    M_Navigation --> X_collections
    M_Compatibility --> X_canonical_product_families
    M_Compatibility --> X_canonical_products
    M_Compatibility --> X_canonical_variants
    M_Compatibility --> X_catalog_sources
    M_Compatibility --> X_source_records
    M_External_mappings --> X_catalog_sources
    M_External_mappings --> X_source_records
    M_Connector_pins --> X_listings
    M_Native_variant_images --> X_listing_images
    M_Native_variant_images --> X_listings
    M_Native_variant_images --> X_product_variants
```

**2 foreign keys point the other way** — from a table outside the epic into
one the epic added. They are the edges that make an epic table impossible to drop quietly:

- `catalog_merge_conflicts.collapsing_relation_id` → `generic_compatibility_relations` (`ON DELETE restrict`)
- `listings.product_type_definition_id` → `product_type_definitions` (`ON DELETE restrict`)

## 2. Write ownership

Measured, not asserted. Every edge below is a `.insert(…)`, `.update(…)` or `.delete(…)` against
the table's drizzle symbol, or a raw-SQL write naming it, found in a non-test module under
`packages/backend/src`. The node on the left is the **directory** the writing module sits in.

**13 modules**, **13 writing directories**,
**4 tables written from more than one directory** and
**4 written by no application code at all**. The exceptions are drawn by name,
because they are the whole reason to look at this graph: `catalog-table-ownership.md` opens with
"one module owns a table", and these are the places where more than one directory issues the
statements.

```mermaid
flowchart LR
    W_db_catalog["db/catalog"]
    W_db_catalogAuthoring["db/catalogAuthoring"]
    W_db_catalogExternalMappings["db/catalogExternalMappings"]
    W_db_catalogGovernance["db/catalogGovernance"]
    W_db_catalogLocalization["db/catalogLocalization"]
    W_db_catalogProposals["db/catalogProposals"]
    W_db_compatibility["db/compatibility"]
    W_db_navigation["db/navigation"]
    W_db_productTypes["db/productTypes"]
    W_db_taxonomy["db/taxonomy"]
    W_db_variantAxes["db/variantAxes"]
    W_scripts_seed_verticals["scripts/seed-verticals"]
    W_services_curation["services/curation"]
    M_Taxonomy(["Taxonomy"])
    M_Classification(["Classification"])
    M_Product_types(["Product types"])
    M_Localization(["Localization"])
    M_Variant_axes_and_claims(["Variant axes and claims"])
    M_Authoring(["Authoring"])
    M_Proposals(["Proposals"])
    M_Governance(["Governance"])
    M_Navigation(["Navigation"])
    M_Compatibility(["Compatibility"])
    M_External_mappings(["External mappings"])
    M_Connector_pins(["Connector pins"])
    M_Native_variant_images(["Native variant images"])
    W_db_catalog -->|"1"| M_Connector_pins
    W_db_catalog -->|"1"| M_Native_variant_images
    W_db_catalogAuthoring -->|"4"| M_Authoring
    W_db_catalogExternalMappings -->|"5"| M_External_mappings
    W_db_catalogGovernance -->|"5"| M_Governance
    W_db_catalogLocalization -->|"5"| M_Localization
    W_db_catalogProposals -->|"2"| M_Authoring
    W_db_catalogProposals -->|"4"| M_Proposals
    W_db_catalogProposals -->|"1"| M_Variant_axes_and_claims
    W_db_compatibility -->|"7"| M_Compatibility
    W_db_navigation -->|"5"| M_Navigation
    W_db_productTypes -->|"4"| M_Product_types
    W_db_taxonomy -->|"2"| M_Classification
    W_db_taxonomy -->|"3"| M_Taxonomy
    W_db_variantAxes -->|"5"| M_Variant_axes_and_claims
    W_scripts_seed_verticals -->|"1"| M_Localization
    W_services_curation -->|"1"| M_Compatibility
    subgraph shared["Written from more than one directory"]
        direction TB
        T_catalog_authoring_draft_values[("catalog_authoring_draft_values")]
        T_catalog_authoring_drafts[("catalog_authoring_drafts")]
        T_generic_compatibility_relations[("generic_compatibility_relations")]
        T_native_listing_attribute_claims[("native_listing_attribute_claims")]
    end
    W_db_catalogAuthoring -.-> T_catalog_authoring_draft_values
    W_db_catalogProposals -.-> T_catalog_authoring_draft_values
    W_db_catalogAuthoring -.-> T_catalog_authoring_drafts
    W_db_catalogProposals -.-> T_catalog_authoring_drafts
    W_db_compatibility -.-> T_generic_compatibility_relations
    W_services_curation -.-> T_generic_compatibility_relations
    W_db_catalogProposals -.-> T_native_listing_attribute_claims
    W_db_variantAxes -.-> T_native_listing_attribute_claims
    subgraph unwritten["No application writer"]
        direction TB
        T_canonical_product_family_localizations[("canonical_product_family_localizations")]
        T_canonical_product_localizations[("canonical_product_localizations")]
        T_catalog_localization_revisions[("catalog_localization_revisions")]
        T_product_type_aliases[("product_type_aliases")]
    end
```

### Tables written from more than one directory

Each of these is a real production call site, not a scanning artefact — the gate binds every
entry to a file it actually read.

| Table | Owning module | Writing directories |
|---|---|---|
| `catalog_authoring_draft_values` | Authoring | `db/catalogAuthoring/draftRepository.ts` (delete/insert/update)<br>`db/catalogProposals/backfillRepository.ts` (update) |
| `catalog_authoring_drafts` | Authoring | `db/catalogAuthoring/draftRepository.ts` (insert/update)<br>`db/catalogProposals/backfillRepository.ts` (update) |
| `generic_compatibility_relations` | Compatibility | `db/compatibility/compatibilityRelationRepository.ts` (insert/update)<br>`services/curation/merge-conflicts.ts` (update) |
| `native_listing_attribute_claims` | Variant axes and claims | `db/catalogProposals/backfillRepository.ts` (update)<br>`db/variantAxes/attributeClaimRepository.ts` (insert/update) |

### Tables no application code writes

Not a gap in the map — a fact about it. A table nothing writes is the one most likely to
acquire a writer nobody argues about, so it is named here rather than left as an absence.

| Table | Owning module | Written by |
|---|---|---|
| `canonical_product_family_localizations` | Localization | a database trigger, or nothing yet |
| `canonical_product_localizations` | Localization | a database trigger, or nothing yet |
| `catalog_localization_revisions` | Localization | a database trigger, or nothing yet |
| `product_type_aliases` | Product types | a database trigger, or nothing yet |

**What this scan cannot see**, stated so an empty result is never read as proof of absence: a
write assembled from a table name held in a variable, a write through an aliased import (the
gate fails the build if one appears), and any statement issued from outside
`packages/backend/src`. `catalog-table-ownership.md` §"Who may write, per subject" carries the
HTTP-reachability half, which no source scan can answer.

## 3. Cardinality, by module

All 56 tables created by a migration at or after `0088`
appear below exactly once, each under the module that owns it. Every relationship is a foreign
key drizzle will emit; the label is the child columns and the `ON DELETE` action.

### Taxonomy

```mermaid
erDiagram
    category_aliases {
    }
    category_redirects {
    }
    category_external_mappings {
    }
    categories ||--o{ category_aliases : "category_id · cascade"
    catalog_sources ||--o{ category_external_mappings : "source_id · restrict"
    categories ||--o{ category_external_mappings : "category_id · restrict"
    categories |o--o{ category_redirects : "subject_category_id · restrict"
    categories ||--o{ category_redirects : "target_category_id · restrict"
```

Also names, from outside the epic: `catalog_sources`, `categories`.

| Table | Created by | Written by |
|---|---|---|
| `category_aliases` | `0088` | `db/taxonomy` (delete/insert) |
| `category_redirects` | `0088` | `db/taxonomy` (insert) |
| `category_external_mappings` | `0088` | `db/taxonomy` (insert/update) |

### Classification

```mermaid
erDiagram
    listing_secondary_categories {
    }
    canonical_product_secondary_categories {
    }
    canonical_products ||--o{ canonical_product_secondary_categories : "canonical_product_id · cascade"
    categories ||--o{ canonical_product_secondary_categories : "category_id · restrict"
    categories ||--o{ listing_secondary_categories : "category_id · restrict"
    listings ||--o{ listing_secondary_categories : "listing_id · cascade"
```

Also names, from outside the epic: `canonical_products`, `categories`, `listings`.

| Table | Created by | Written by |
|---|---|---|
| `listing_secondary_categories` | `0134` | `db/taxonomy` (insert) |
| `canonical_product_secondary_categories` | `0134` | `db/taxonomy` (insert) |

### Product types

```mermaid
erDiagram
    product_type_definitions {
    }
    product_type_category_scopes {
    }
    product_type_field_groups {
    }
    product_type_fields {
    }
    product_type_aliases {
    }
    product_type_definitions ||--o{ product_type_aliases : "product_type_definition_id · cascade"
    categories ||--o{ product_type_category_scopes : "category_id · restrict"
    product_type_definitions ||--o{ product_type_category_scopes : "product_type_definition_id · cascade"
    product_type_definitions ||--o{ product_type_field_groups : "product_type_definition_id · cascade"
    attribute_definitions ||--o{ product_type_fields : "attribute_definition_id · restrict"
    product_type_definitions ||--o{ product_type_fields : "product_type_definition_id · cascade"
    product_type_field_groups |o--o{ product_type_fields : "group_id + product_type_definition_id · no action"
```

Also names, from outside the epic: `attribute_definitions`, `categories`.

| Table | Created by | Written by |
|---|---|---|
| `product_type_definitions` | `0089` | `db/productTypes` (insert/update) |
| `product_type_category_scopes` | `0089` | `db/productTypes` (insert) |
| `product_type_field_groups` | `0089` | `db/productTypes` (insert) |
| `product_type_fields` | `0089` | `db/productTypes` (delete/insert) |
| `product_type_aliases` | `0112` | — *no application writer* |

### Localization

```mermaid
erDiagram
    category_localizations {
    }
    category_localized_slugs {
    }
    product_type_localizations {
    }
    product_type_field_localizations {
    }
    attribute_value_localizations {
    }
    listing_localizations {
    }
    canonical_product_localizations {
    }
    canonical_product_family_localizations {
    }
    catalog_localization_revisions {
    }
    attribute_enum_values ||--o{ attribute_value_localizations : "attribute_enum_value_id · cascade"
    canonical_product_families ||--o{ canonical_product_family_localizations : "canonical_product_family_id · cascade"
    canonical_products ||--o{ canonical_product_localizations : "canonical_product_id · cascade"
    catalog_localization_revisions |o--o{ catalog_localization_revisions : "rollback_of_revision_id · restrict"
    categories ||--o{ category_localizations : "category_id · cascade"
    categories ||--o{ category_localized_slugs : "category_id · cascade"
    category_localized_slugs |o--o{ category_localized_slugs : "superseded_by_slug_id · restrict"
    listings ||--o{ listing_localizations : "listing_id · cascade"
    product_type_fields ||--o{ product_type_field_localizations : "product_type_field_id · cascade"
    product_type_definitions ||--o{ product_type_localizations : "product_type_definition_id · cascade"
```

Also names, from other #367 modules: `product_type_definitions`, `product_type_fields`.

Also names, from outside the epic: `attribute_enum_values`, `canonical_product_families`, `canonical_products`, `categories`, `listings`.

| Table | Created by | Written by |
|---|---|---|
| `category_localizations` | `0091` | `db/catalogLocalization` (insert) |
| `category_localized_slugs` | `0091` | `db/catalogLocalization` (insert/update) |
| `product_type_localizations` | `0091` | `db/catalogLocalization` (insert) |
| `product_type_field_localizations` | `0112` | `db/catalogLocalization` (insert) |
| `attribute_value_localizations` | `0091` | `scripts/seed-verticals` (insert) |
| `listing_localizations` | `0130` | `db/catalogLocalization` (delete/insert) |
| `canonical_product_localizations` | `0120` | — *no application writer* |
| `canonical_product_family_localizations` | `0120` | — *no application writer* |
| `catalog_localization_revisions` | `0118` | — *no application writer* |

### Variant axes and claims

```mermaid
erDiagram
    native_listing_variant_axes {
    }
    native_variant_axis_assignments {
    }
    native_variant_signatures {
    }
    native_listing_attribute_claims {
    }
    native_variant_attribute_claims {
    }
    attribute_definitions |o--o{ native_listing_attribute_claims : "attribute_definition_id · restrict"
    attribute_enum_values |o--o{ native_listing_attribute_claims : "enum_value_id · restrict"
    connections |o--o{ native_listing_attribute_claims : "source_connection_id · restrict"
    listings ||--o{ native_listing_attribute_claims : "listing_id · cascade"
    attribute_definitions ||--o{ native_listing_variant_axes : "attribute_definition_id · restrict"
    listings ||--o{ native_listing_variant_axes : "listing_id · cascade"
    product_type_definitions |o--o{ native_listing_variant_axes : "product_type_definition_id · restrict"
    attribute_definitions |o--o{ native_variant_attribute_claims : "attribute_definition_id · restrict"
    attribute_enum_values |o--o{ native_variant_attribute_claims : "enum_value_id · restrict"
    connections |o--o{ native_variant_attribute_claims : "source_connection_id · restrict"
    product_variants ||--o{ native_variant_attribute_claims : "variant_id · cascade"
    attribute_definitions ||--o{ native_variant_axis_assignments : "attribute_definition_id · restrict"
    attribute_enum_values |o--o{ native_variant_axis_assignments : "enum_value_id · restrict"
    native_listing_variant_axes ||--o{ native_variant_axis_assignments : "axis_id · cascade"
    product_variants ||--o{ native_variant_axis_assignments : "variant_id · cascade"
    listings ||--o{ native_variant_signatures : "listing_id · cascade"
    product_variants ||--o| native_variant_signatures : "variant_id · cascade"
```

Also names, from other #367 modules: `product_type_definitions`.

Also names, from outside the epic: `attribute_definitions`, `attribute_enum_values`, `connections`, `listings`, `product_variants`.

| Table | Created by | Written by |
|---|---|---|
| `native_listing_variant_axes` | `0097` | `db/variantAxes` (delete/insert) |
| `native_variant_axis_assignments` | `0097` | `db/variantAxes` (delete/insert) |
| `native_variant_signatures` | `0097` | `db/variantAxes` (insert) |
| `native_listing_attribute_claims` | `0097` | `db/catalogProposals` (update)<br>`db/variantAxes` (insert/update) |
| `native_variant_attribute_claims` | `0097` | `db/variantAxes` (insert/update) |

### Authoring

```mermaid
erDiagram
    catalog_authoring_drafts {
    }
    catalog_authoring_draft_variants {
    }
    catalog_authoring_draft_values {
    }
    catalog_authoring_schema_invalidations {
    }
    attribute_definitions ||--o{ catalog_authoring_draft_values : "attribute_definition_id · restrict"
    attribute_enum_values |o--o{ catalog_authoring_draft_values : "value_enum_value_id · cascade"
    catalog_authoring_draft_variants |o--o{ catalog_authoring_draft_values : "draft_variant_id · cascade"
    catalog_authoring_drafts ||--o{ catalog_authoring_draft_values : "draft_id · cascade"
    product_type_fields ||--o{ catalog_authoring_draft_values : "field_id · restrict"
    catalog_authoring_drafts ||--o{ catalog_authoring_draft_variants : "draft_id · cascade"
    categories ||--o{ catalog_authoring_drafts : "category_id · restrict"
    listings |o--o{ catalog_authoring_drafts : "published_listing_id · restrict"
    product_type_definitions ||--o{ catalog_authoring_drafts : "product_type_definition_id · restrict"
    stores ||--o{ catalog_authoring_drafts : "store_id · cascade"
```

Also names, from other #367 modules: `product_type_definitions`, `product_type_fields`.

Also names, from outside the epic: `attribute_definitions`, `attribute_enum_values`, `categories`, `listings`, `stores`.

| Table | Created by | Written by |
|---|---|---|
| `catalog_authoring_drafts` | `0098` | `db/catalogAuthoring` (insert/update)<br>`db/catalogProposals` (update) |
| `catalog_authoring_draft_variants` | `0098` | `db/catalogAuthoring` (delete/insert/update) |
| `catalog_authoring_draft_values` | `0098` | `db/catalogAuthoring` (delete/insert/update)<br>`db/catalogProposals` (update) |
| `catalog_authoring_schema_invalidations` | `0098` | `db/catalogAuthoring` (insert) |

### Proposals

```mermaid
erDiagram
    catalog_proposals {
    }
    catalog_proposal_duplicate_candidates {
    }
    catalog_proposal_references {
    }
    catalog_review_events {
    }
    catalog_proposals ||--o{ catalog_proposal_duplicate_candidates : "proposal_id · cascade"
    catalog_authoring_draft_values |o--o{ catalog_proposal_references : "draft_value_id · cascade"
    catalog_authoring_drafts |o--o{ catalog_proposal_references : "draft_id · cascade"
    catalog_proposals ||--o{ catalog_proposal_references : "proposal_id · cascade"
    native_listing_attribute_claims |o--o{ catalog_proposal_references : "listing_claim_id · cascade"
    attribute_definitions |o--o{ catalog_proposals : "attribute_definition_id · restrict"
    catalog_proposals |o--o{ catalog_proposals : "redirected_to_proposal_id · restrict"
    categories |o--o{ catalog_proposals : "category_id · restrict"
    product_type_definitions |o--o{ catalog_proposals : "product_type_definition_id · restrict"
    stores |o--o{ catalog_proposals : "store_id · restrict"
    catalog_proposals ||--o{ catalog_review_events : "proposal_id · restrict"
```

Also names, from other #367 modules: `catalog_authoring_draft_values`, `catalog_authoring_drafts`, `native_listing_attribute_claims`, `product_type_definitions`.

Also names, from outside the epic: `attribute_definitions`, `categories`, `stores`.

| Table | Created by | Written by |
|---|---|---|
| `catalog_proposals` | `0100` | `db/catalogProposals` (insert/update) |
| `catalog_proposal_duplicate_candidates` | `0100` | `db/catalogProposals` (insert) |
| `catalog_proposal_references` | `0100` | `db/catalogProposals` (insert/update) |
| `catalog_review_events` | `0100` | `db/catalogProposals` (insert) |

### Governance

```mermaid
erDiagram
    catalog_governance_change_requests {
    }
    catalog_governance_impact_counts {
    }
    catalog_governance_audit_events {
    }
    catalog_governance_role_grants {
    }
    catalog_governance_definition_snapshots {
    }
    catalog_governance_change_requests |o--o{ catalog_governance_audit_events : "change_request_id · restrict"
    catalog_governance_change_requests ||--o{ catalog_governance_impact_counts : "change_request_id · restrict"
```

| Table | Created by | Written by |
|---|---|---|
| `catalog_governance_change_requests` | `0102` | `db/catalogGovernance` (insert/update) |
| `catalog_governance_impact_counts` | `0102` | `db/catalogGovernance` (insert) |
| `catalog_governance_audit_events` | `0102` | `db/catalogGovernance` (insert) |
| `catalog_governance_role_grants` | `0102` | `db/catalogGovernance` (insert/update) |
| `catalog_governance_definition_snapshots` | `0102` | `db/catalogGovernance` (insert) |

### Navigation

```mermaid
erDiagram
    navigation_saved_queries {
    }
    navigation_saved_query_attribute_filters {
    }
    navigation_trees {
    }
    navigation_nodes {
    }
    navigation_node_localizations {
    }
    navigation_nodes ||--o{ navigation_node_localizations : "node_id · cascade"
    brands |o--o{ navigation_nodes : "brand_id · restrict"
    canonical_product_families |o--o{ navigation_nodes : "product_family_id · restrict"
    categories |o--o{ navigation_nodes : "category_id · restrict"
    collections |o--o{ navigation_nodes : "collection_id · restrict"
    navigation_nodes |o--o{ navigation_nodes : "parent_id · cascade"
    navigation_saved_queries |o--o{ navigation_nodes : "saved_query_id · restrict"
    navigation_trees ||--o{ navigation_nodes : "tree_id · cascade"
    categories |o--o{ navigation_saved_queries : "category_id · restrict"
    navigation_saved_queries ||--o{ navigation_saved_query_attribute_filters : "saved_query_id · cascade"
    navigation_trees |o--o{ navigation_trees : "supersedes_tree_id · restrict"
```

Also names, from outside the epic: `brands`, `canonical_product_families`, `categories`, `collections`.

| Table | Created by | Written by |
|---|---|---|
| `navigation_saved_queries` | `0090` | `db/navigation` (insert) |
| `navigation_saved_query_attribute_filters` | `0090` | `db/navigation` (insert) |
| `navigation_trees` | `0090` | `db/navigation` (delete/insert/update) |
| `navigation_nodes` | `0090` | `db/navigation` (delete/insert) |
| `navigation_node_localizations` | `0090` | `db/navigation` (insert) |

### Compatibility

```mermaid
erDiagram
    generic_compatibility_relations {
    }
    vehicle_makes {
    }
    vehicle_models {
    }
    vehicle_generations {
    }
    vehicle_configurations {
    }
    automotive_fitments {
    }
    compatibility_claims {
    }
    automotive_fitments |o--o{ automotive_fitments : "superseded_by_id · restrict"
    canonical_products |o--o{ automotive_fitments : "subject_product_id · restrict"
    canonical_variants |o--o{ automotive_fitments : "subject_variant_id · restrict"
    catalog_sources |o--o{ automotive_fitments : "asserted_by_source_id · restrict"
    source_records |o--o{ automotive_fitments : "source_record_id · restrict"
    vehicle_configurations |o--o{ automotive_fitments : "vehicle_configuration_id · restrict"
    vehicle_generations |o--o{ automotive_fitments : "vehicle_generation_id · restrict"
    vehicle_makes ||--o{ automotive_fitments : "vehicle_make_id · restrict"
    vehicle_models |o--o{ automotive_fitments : "vehicle_model_id · restrict"
    automotive_fitments |o--o{ compatibility_claims : "fitment_id · restrict"
    canonical_products |o--o{ compatibility_claims : "subject_product_id · restrict"
    canonical_variants |o--o{ compatibility_claims : "subject_variant_id · restrict"
    catalog_sources |o--o{ compatibility_claims : "asserted_by_source_id · restrict"
    generic_compatibility_relations |o--o{ compatibility_claims : "relation_id · restrict"
    source_records |o--o{ compatibility_claims : "source_record_id · restrict"
    canonical_product_families |o--o{ generic_compatibility_relations : "target_family_id · restrict"
    canonical_products |o--o{ generic_compatibility_relations : "subject_product_id · restrict"
    canonical_products |o--o{ generic_compatibility_relations : "target_product_id · restrict"
    canonical_variants |o--o{ generic_compatibility_relations : "subject_variant_id · restrict"
    canonical_variants |o--o{ generic_compatibility_relations : "target_variant_id · restrict"
    catalog_sources |o--o{ generic_compatibility_relations : "asserted_by_source_id · restrict"
    generic_compatibility_relations |o--o{ generic_compatibility_relations : "superseded_by_id · restrict"
    vehicle_configurations |o--o{ vehicle_configurations : "merged_into_id · restrict"
    vehicle_generations ||--o{ vehicle_configurations : "generation_id · restrict"
    vehicle_generations |o--o{ vehicle_generations : "merged_into_id · restrict"
    vehicle_models ||--o{ vehicle_generations : "model_id · restrict"
    vehicle_makes |o--o{ vehicle_makes : "merged_into_id · restrict"
    vehicle_makes ||--o{ vehicle_models : "make_id · restrict"
    vehicle_models |o--o{ vehicle_models : "merged_into_id · restrict"
```

Also names, from outside the epic: `canonical_product_families`, `canonical_products`, `canonical_variants`, `catalog_sources`, `source_records`.

| Table | Created by | Written by |
|---|---|---|
| `generic_compatibility_relations` | `0093` | `db/compatibility` (insert/update)<br>`services/curation` (update) |
| `vehicle_makes` | `0093` | `db/compatibility` (insert) |
| `vehicle_models` | `0093` | `db/compatibility` (insert) |
| `vehicle_generations` | `0093` | `db/compatibility` (insert) |
| `vehicle_configurations` | `0093` | `db/compatibility` (insert) |
| `automotive_fitments` | `0093` | `db/compatibility` (insert/update) |
| `compatibility_claims` | `0093` | `db/compatibility` (insert/update) |

### External mappings

```mermaid
erDiagram
    catalog_external_mappings {
    }
    catalog_external_mapping_reviews {
    }
    catalog_external_token_observations {
    }
    catalog_external_mapping_runs {
    }
    catalog_external_mapping_run_items {
    }
    catalog_external_mappings |o--o{ catalog_external_mapping_reviews : "resolved_mapping_id · restrict"
    catalog_sources ||--o{ catalog_external_mapping_reviews : "catalog_source_id · restrict"
    source_records |o--o{ catalog_external_mapping_reviews : "source_record_id · restrict"
    catalog_external_mapping_runs ||--o{ catalog_external_mapping_run_items : "run_id · cascade"
    catalog_external_mappings |o--o{ catalog_external_mapping_run_items : "next_mapping_id · restrict"
    catalog_external_mappings |o--o{ catalog_external_mapping_run_items : "previous_mapping_id · restrict"
    catalog_external_mappings |o--o{ catalog_external_mapping_runs : "mapping_id · restrict"
    catalog_sources ||--o{ catalog_external_mapping_runs : "catalog_source_id · restrict"
    catalog_external_mappings |o--o{ catalog_external_mappings : "supersedes_mapping_id · restrict"
    catalog_sources ||--o{ catalog_external_mappings : "catalog_source_id · restrict"
    product_type_definitions |o--o{ catalog_external_mappings : "reviewed_product_type_definition_id · restrict"
    source_records |o--o{ catalog_external_mappings : "evidence_source_record_id · restrict"
    catalog_external_mappings |o--o{ catalog_external_token_observations : "resolved_mapping_id · restrict"
    catalog_sources ||--o{ catalog_external_token_observations : "catalog_source_id · restrict"
```

Also names, from other #367 modules: `product_type_definitions`.

Also names, from outside the epic: `catalog_sources`, `source_records`.

| Table | Created by | Written by |
|---|---|---|
| `catalog_external_mappings` | `0094` | `db/catalogExternalMappings` (insert/update) |
| `catalog_external_mapping_reviews` | `0094` | `db/catalogExternalMappings` (insert/update) |
| `catalog_external_token_observations` | `0094` | `db/catalogExternalMappings` (insert/update) |
| `catalog_external_mapping_runs` | `0094` | `db/catalogExternalMappings` (insert/update) |
| `catalog_external_mapping_run_items` | `0094` | `db/catalogExternalMappings` (insert) |

### Connector pins

```mermaid
erDiagram
    listing_pin_releases {
    }
    listings ||--o{ listing_pin_releases : "listing_id · cascade"
```

Also names, from outside the epic: `listings`.

| Table | Created by | Written by |
|---|---|---|
| `listing_pin_releases` | `0099` | `db/catalog` (insert) |

### Native variant images

```mermaid
erDiagram
    product_variant_images {
    }
    listing_images ||--o{ product_variant_images : "listing_image_id + listing_id · cascade"
    listings ||--o{ product_variant_images : "listing_id · cascade"
    product_variants ||--o{ product_variant_images : "variant_id + listing_id · cascade"
```

Also names, from outside the epic: `listing_images`, `listings`, `product_variants`.

| Table | Created by | Written by |
|---|---|---|
| `product_variant_images` | `0133` | `db/catalog` (delete/insert) |

## 4. What the derivation found

Counts, so that a diagram that quietly stopped measuring anything is visible as a number that
moved. Every one of these is re-derived on each run and floored by the gate.

| Fact | Value |
|---|---|
| Tables in the population | 56 |
| Modules they are grouped into | 13 |
| Foreign keys out of an epic table | 124 |
| Foreign keys into one, from outside the epic | 2 |
| Pre-existing tables the epic attaches to | 15 |
| Relationships the schema proves are 1:1 | 1 |
| Relationships whose parent is optional (nullable FK) | 66 |
| Directories that write an epic table | 13 |
| Tables written from more than one directory | 4 |
| Tables no application code writes | 4 |

`ON DELETE` across those 124 foreign keys: **41** `cascade`, **1** `no action`, **82** `restrict`.

### The relationships the schema proves are 1:1

- `product_variants` → `native_variant_signatures` on `variant_id`, because a unique constraint covers those columns.
